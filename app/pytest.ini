[pytest]
# The app uses flat imports (`from db import ...`), matching how uvicorn runs
# it from inside app/. pythonpath makes pytest resolve them the same way, so
# tests import the modules exactly as production does.
pythonpath = .
testpaths = tests
